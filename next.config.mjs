/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['gravatar.com'],
    },
  };
  
  export default nextConfig;
//   module.exports = nextConfig;  // Ensure consistency with module exports
  